```
__global__ void vector_add(const float* A, const float* B, float* C, int N) { int thread_id = blockDim.x * blockIdx.x + threadIdx.x; // 1d grid if (thread_id < N) { C[thread_id] = A[thread_id] + B[thread_id]; } }
```

```
//
// Generated by NVIDIA NVVM Compiler
//
// Compiler Build ID: CL-36037853
// Cuda compilation tools, release 12.9, V12.9.86
// Based on NVVM 7.0.1
//

.version 8.8        // PTX ISA version
.target sm_90       // target GPU architecture (Hopper)
.address_size 64    // 64-bit addressing

// Kernel entry point
.visible .entry _Z10vector_addPKfS0_Pfi(
    // kernel parameters passed in constant parameter space
    .param .u64 _Z10vector_addPKfS0_Pfi_param_0,   // const float* A
    .param .u64 _Z10vector_addPKfS0_Pfi_param_1,   // const float* B
    .param .u64 _Z10vector_addPKfS0_Pfi_param_2,   // float* C
    .param .u32 _Z10vector_addPKfS0_Pfi_param_3    // int N
)
{
    // register declarations

    .reg .pred  %p<2>;    // predicate registers (boolean flags for branches)
    .reg .f32   %f<4>;    // floating point registers
    .reg .b32   %r<6>;    // 32-bit integer registers
    .reg .b64   %rd<11>;  // 64-bit integer registers (used for pointers)

    //------------------------------------------------------------
    // Load kernel parameters
    //------------------------------------------------------------

    ld.param.u64  %rd1, [_Z10vector_addPKfS0_Pfi_param_0];  // rd1 = A pointer
    ld.param.u64  %rd2, [_Z10vector_addPKfS0_Pfi_param_1];  // rd2 = B pointer
    ld.param.u64  %rd3, [_Z10vector_addPKfS0_Pfi_param_2];  // rd3 = C pointer
    ld.param.u32  %r2,  [_Z10vector_addPKfS0_Pfi_param_3];  // r2 = N

    //------------------------------------------------------------
    // Compute thread_id = blockDim.x * blockIdx.x + threadIdx.x
    //------------------------------------------------------------

    mov.u32  %r3, %ntid.x;   // r3 = blockDim.x   (# threads per block)
    mov.u32  %r4, %ctaid.x;  // r4 = blockIdx.x   (block index)
    mov.u32  %r5, %tid.x;    // r5 = threadIdx.x  (thread index inside block)

    mad.lo.s32 %r1, %r3, %r4, %r5;
    // r1 = r3*r4 + r5
    // thread_id = blockDim.x * blockIdx.x + threadIdx.x

    //------------------------------------------------------------
    // Bounds check: if (thread_id >= N) return
    //------------------------------------------------------------

    setp.ge.s32 %p1, %r1, %r2;   // p1 = (thread_id >= N)

    @%p1 bra $L__BB0_2;          // if p1 true, branch to exit

    //------------------------------------------------------------
    // Compute address of A[thread_id]
    //------------------------------------------------------------

    cvta.to.global.u64 %rd4, %rd1;
    // convert generic pointer to global memory address space

    mul.wide.s32 %rd5, %r1, 4;
    // rd5 = thread_id * sizeof(float)
    // widen to 64-bit to compute byte offset

    add.s64 %rd6, %rd4, %rd5;
    // rd6 = &A[thread_id]

    //------------------------------------------------------------
    // Compute address of B[thread_id]
    //------------------------------------------------------------

    cvta.to.global.u64 %rd7, %rd2;
    add.s64 %rd8, %rd7, %rd5;
    // rd8 = &B[thread_id]

    //------------------------------------------------------------
    // Load A[i] and B[i]
    //------------------------------------------------------------

    ld.global.f32 %f1, [%rd8];   // f1 = B[thread_id]
    ld.global.f32 %f2, [%rd6];   // f2 = A[thread_id]

    //------------------------------------------------------------
    // Compute addition
    //------------------------------------------------------------

    add.f32 %f3, %f2, %f1;       // f3 = A[i] + B[i]

    //------------------------------------------------------------
    // Compute address of C[thread_id]
    //------------------------------------------------------------

    cvta.to.global.u64 %rd9, %rd3;
    add.s64 %rd10, %rd9, %rd5;
    // rd10 = &C[thread_id]

    //------------------------------------------------------------
    // Store result
    //------------------------------------------------------------

    st.global.f32 [%rd10], %f3;  // C[i] = result

$L__BB0_2:
    ret;   // kernel return
}
```

## Convert ptx instruction ##
The `cvta.to.global.u64` is a type casting instruction that converts a float to a global address-space type. This conversion only exists in ptx and does not get executed as an instruction

## Redudnant work: thread_id ##
Note that thread_id is recomputed for each kernel launch, rather than be pre-computed for each thread. 